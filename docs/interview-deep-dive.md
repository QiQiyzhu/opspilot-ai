# OpsPilot：从一个检索失败讲清业务权限

**本轮升级的 30 秒讲法：**“真实模型返回合法 JSON 后，客服仍可能不知道下一步该做什么。我先冻结 24 条合成案例，再将分类定义和服务端分流分别消融：16 条测试的下一步匹配是 12、13、13、16。只有提示词或只有工作流都不够。完整 24 条仍错一条，且标签由 AI 辅助编写，没有独立盲测资格。产品改为在工具前明确澄清或人工政策审阅；它不把客户偏好当作审批。”[实际界面、48 次原始调用与完整限制](competition-upgrade.md)。

[完整案例](decision-case-study.md) · [逐条证据](../evals/reports/decision-case.json) · [核心实现](../backend/business.py)

**真实模型增量：**另有 DeepSeek Flash 的 6 次真实意图调用，5/6 标签匹配，1 次把 ambiguous 分为 policy_conflict。它揭示“JSON 合法但语义错误”；不替代下方历史检索/事务实验，也不构成完整 Agent 评测。[原始输出、tokens、来源和失败分析](real-model-results.md)。

## 30 秒版本

“我把模型理解、证据检索和业务授权分开验证。真实 BGE 检索的 4 条无答案只拒答 1 条；DeepSeek 的 6 次真实调用也有一次 JSON 合法但分类错。它们都不能授权退款。执行必须经过角色、当前政策、事务与独立验证；真实 PostgreSQL 证明待审零退款、越权 403、重放一条账及资格过期 409。业务是模拟的，六条模型开发例不能代表生产准确率。”

## 3 分钟版本

**第一分钟：先承认失败。** 打开 `rag_29`，读问题、gold 无答案与返回段落，说明它不是语言模型幻觉测试，而是实际检索拒答失败。再切 `rag_02`：hybrid 第 1、rerank 第 4，解释为什么不能只按 MRR 选系统。原始 report 保留 120 行，不只挑成功例。

**第二分钟：展示一道真实边界。** 打开 `boundary_execution.steps`。同一套可执行政策下，检索命中与 31 天订单的 409 同时成立。合格订单的提案没有改业务状态，操作员也不能批准。审阅员执行后读取独立 verifier，核对批准金额 12900 分、记录/订单关联，而不是只读工具返回的“success”。同 key 重放只有 1 refund 与 1 audit。

**第三分钟：讲被拒方案和代价。** 分数阈值不能定义操作权限；第二个 Agent 也不能授予它。六次真实 DeepSeek 意图调用的错例说明 schema 也不能替代语义评测。保留人工门槛带来等待，规则和文档需要版本管理。下一步是独立留出集与人工审阅；不能把开发集调全过就宣布完成。真实模型完整 Agent ablation、真实退款到账和生产吞吐仍没有执行证据。

## 8 分钟深挖路线

| 时间 | 打开什么 | 需要讲清的推理 |
| --- | --- | --- |
| 0:00–1:00 | 数据集、`provenance` | 26 可答+4 无答案；AI-reviewed；本地真实 ONNX；不是用户研究 |
| 1:00–2:15 | `rag_29` 四种结果 | 词汇 OR 向量阈值如何放进错误上下文；这是观察和机制解释，不是模型因果实验 |
| 2:15–3:15 | `rag_02` 与汇总表 | MRR 与 K3 覆盖的取舍；保留 K5 的有限理由；不能比较跨算法分数大小 |
| 3:15–4:30 | `propose`、`eligibility_in_session` | 模型仅建议；当前订单、年龄、可执行政策决定资格；提案保留证据和参数 |
| 4:30–5:45 | `_decide_transaction` | 审阅身份来自服务端；run→proposal→order 锁顺序；执行时复查；账本和审计原子提交 |
| 5:45–6:45 | 实测 stale-eligibility 与 verifier | 只改变签收时间也能拒绝旧提案；核对账本金额/关联；submitted 不等于银行结算 |
| 6:45–7:20 | 已有并发/变异测试 | 顺序演示与并发证据分开；正确性结论限定到被验证不变量 |
| 7:20–8:00 | 下一实验与开放项 | 拒答仍弱；保持人审；先独立留出标签再校准，不承诺通用准确率 |

## 被拒方案、反例和取舍

- **“MRR 高就自动执行”**：`rag_29` 的返回结果直接反驳；检索目标和授权目标不同。
- **“把 rerank 去掉就解决了”**：hybrid K3 较强，但 MRR 更低，无答案仍 1/4。没有一种模式在当前所有维度占优。
- **“只要版本没变，就信已批准快照”**：资格随时间变化；新增回归不修改 order.version 也应拒绝执行。
- **“全部交给第二个 Agent 审查”**：现有 Fake full 与 multi 同为 59/60，只能说明这套脚本下没有增益，不能得出真实多 Agent 无用；更不能替代权限控制。
- **“库存 verifier 比当前库存就够”**：其他订单会合法改库存。已有实现校验提案专属 movement 的数量与前后算术，限定在已提交预留证据。

## 五个追问

1. **为什么无答案不混进 Recall？** 无 gold 的问题没有相关段落排名；混入会掩盖拒答失败。因此 26 条计算 Recall/MRR，4 条单列拒答，数量太小不做显著性结论。
2. **词汇分数大于零为什么危险？** 当前 OR 过滤允许任何正词汇信号进入，即使语义很弱。调整需要新的近领域反例和误拒答权衡；这里没有声称已修好。
3. **审批开始后订单又变化怎么办？** 执行事务获取锁后重新读资格/版本/政策。取消还与审批共享 run 级线性化顺序；实际 barrier 测试验证两种顺序，不能仅检查 cancel 先结束的情况。
4. **数据库提交了，但用户没有收到响应怎么办？** 重放按幂等 key 返回已有提案，再独立验证；本演示观察到账本和审计各一条。持久任务调度和真实外部支付幂等仍是后续工作。
5. **你到底证明了什么？** 证明特定模拟订单的服务端防线真实生效、历史开发检索确有取舍。没有证明 LLM 推理效果、完整生产安全、真实结算或业务 ROI。

可追加 30 秒：“真实模型不是换个按钮就完成升级。我先限制单次输出、总超时和付费尝试数，再保留 request ID、实际 usage 和失败类别。`ambiguous` 被分为 `policy_conflict` 的错例通过了 schema，说明下一步要先冻结标签定义和留出集，再比较模型；不能改预期答案来追求 100%。”

## 下一实验与讲述边界

按[案例的预备协议](decision-case-study.md#5-复现与下一次能推翻本决策的实验)冻结校准/留出集，保留同意流程和人工标签状态。若门槛提升拒答却使可答覆盖明显下降，保留现状或把更多请求转人工；若任何安全不变量失败，先阻止执行发布，不用平均分抵消。研究和实验仍标记计划。

以上为 AI-assisted 工程案例，不是代写工作经历。只有本人读懂调用链、能现场解释原始失败并复现实验后，才使用第一人称讲述。
